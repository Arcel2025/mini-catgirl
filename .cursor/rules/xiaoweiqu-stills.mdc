---
description: 小委屈是两套游戏。先认是哪一套，再出图或改代码。
alwaysApply: true
---

# 小委屈：两套游戏

同一只角色，不是同一个游戏。文件夹现在还挤在 `养成游戏-迷你猫娘-小委屈/`，改之前先认是哪一套。不要把剧情分镜做成养成系统，也不要把体力条写进第一章。

| | 线性剧情 | 体力养成 |
| --- | --- | --- |
| 玩什么 | 点一下下一句 | 房间里过日子 |
| 文档 | `小委屈故事.md`、`app.py` | `html日常.md`、`web/` |
| 图 | 9:16 静帧 | `864×1152` 贴纸 |
| 数字 | 没有体力条 | 饥饿、清洁、心情、健康、体力 |
| 对照 | 分镜海报 | 小红书纸箱猫房间页 |

人设共用 `小委屈.md`。头重摔跤留给别的迷你猫娘；她日常不演。养成难做循环就出动感静帧、网页平移。

养成贴纸先把要做什么记进 `html日常.md`，用户点头后再一次性出图，好对齐头宽和比例。不要听一条就 `GenerateImage`，也不要先改进游戏。

物理拆目录等用户点头。未点头不要推远程。

## 剧情出图：先问清再出图

用户说剧情、要新 9:16、或改窗口第一章时：先反馈缺的画面，等他说清，再 `GenerateImage`。五句都齐了才出图。不要一听剧情就出图。

1. **镜头**：全景 / 局部放大 / 第一人称。只说「她很小」不够；小是镜头的结果。
2. **底图**：用哪张已定稿当场地。新地方先锁空景，再放人。
3. **位置**：圈已定稿的图，或写清「谁旁边、哪两块砖」。
4. **对照**：已定稿图，或用户找来的现实照片、其他 AI 图。用来钉动作和大小（人、箱、盆、手、台阶）。厘米是验收，不是唯一指令。
5. **只改什么**：哪几样已经对了，不许动。一次只改进一个变量。

缺哪句就问哪句。已齐的不要再问。用户圈图、标错、对照图，比再念一遍尺子优先。

用户丢来的现实图 / 其他 AI 图：先给人看，抽出镜头和比例，再出图。角色仍锁本库定稿（`idle` 或 `ch1-standlook`），不要把别人的脸锁进来。带框带箭头的只给人看。这些对照图不要进公开库，除非用户点头。

## 养成贴纸

走 `html日常.md` 和 [贴纸动画提示词](../skills/mini-catgirl-anim-prompt/SKILL.md)。不要套上面五句里的 9:16 场地。锁 `web/assets/stand.png`。默认静帧。不要调用 Cursor `GenerateImage` 拆帧。清单没锁完、用户没说「出图」之前，只记不画。

## 剧情出图手续

- 锁图最多两张：一张对照（场地、动作或比例），一张本库角色。路径用仓库根目录英文名 `tmp-*.jpg`。
- 带箭头、红框的标注图只给人看，不要当 `reference_image_paths`。
- 提示词禁止写星野、Xingye、MiniMax、Talkie 及官方画风商品名。
- 第一章静帧 `9:16`（720×1280）。空阳台、阳台角落等已定稿不要重出，除非用户再说。
- 她约 20cm；第一人称还要先定手占画面多少、她只占指间。
- 点头定稿后：拷进 `packs/xiaoweiqu/assets/`，登记 `pet.json` / `options.json`，改 `小委屈故事.md`。
