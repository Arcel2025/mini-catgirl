---
description: 小委屈剧情先把画面说清楚，再出图，再改游戏
alwaysApply: true
---

# 小委屈剧情图：先问清再出图

用户说剧情、要新静帧、或改 `养成游戏-迷你猫娘-小委屈/` 时：先反馈缺的画面，等他说清，再 `GenerateImage`，再改进窗口和文档。五句都齐了才出图。不要一听剧情就出图。

## 先跟用户对这五句

1. **镜头**：全景 / 局部放大 / 第一人称。只说「她很小」不够；小是镜头的结果。
2. **底图**：用哪张已定稿当场地。新地方先锁空景，再放人。
3. **位置**：圈已定稿的图，或写清「谁旁边、哪两块砖」。
4. **对照**：哪张已合格图的大小关系（人、箱、盆、手）。厘米是验收，不是唯一指令。
5. **只改什么**：哪几样已经对了，不许动。一次只改进一个变量。

缺哪句就问哪句。已齐的不要再问。用户圈图、标错、对照旧图，比再念一遍尺子优先。

## 出图和改游戏

- 锁图最多两张：一张场地或风格，一张角色。路径用仓库根目录英文名 `tmp-*.jpg`。
- 带箭头、红框的标注图只给人看，不要当 `reference_image_paths`。
- 提示词禁止写星野、Xingye、MiniMax、Talkie 及官方画风商品名。
- 第一章静帧 `9:16`（720×1280）。空阳台、阳台角落等已定稿不要重出，除非用户再说。
- 她约 20cm；第一人称还要先定手占画面多少、她只占指间。
- 点头定稿后：拷进 `packs/xiaoweiqu/assets/`，登记 `pet.json` / `options.json`，改 `小委屈故事.md`。未点头不要推远程。
